# StackWise Database

PostgreSQL is started through Docker Compose. Seed JSON files in `Database/seeds` provide the technology knowledge base, recommendation rules, compatibility rules, and feature rules.
